{
  "name": "MPorT Development",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "forwardPorts": [80,3306],
  "postCreateCommand": "composer install || true && npm install || true"
}
